<div class="panel panel-default">
	<div class="panel-heading">
		<h4 class="panel-title">
			<a class="accordion-toggle cf" data-toggle="collapse" data-parent="#product-accordion" href="#collapse{{value}}">
				<span class="pull-left">{{name}}</span>
				<i class="icon-chevron-right pull-right"></i>
				<i class="icon-chevron-down pull-right hide"></i>
			</a>
		</h4>
	</div>
	<div id="collapse{{value}}" class="panel-collapse collapse">
		<div class="panel-body">
			<p>Click here for a <a href="#" class="style-names" data-target="{{target}}" data-style="katakana">Name in Katakana</a> or here for <a href="#" class="style-names" data-target="{{target}}" data-style="kanji">Kanji Style Names</a></p>
			<p><input type="text" name="{{target}}" class="embroidery" data-id="{{value}}"></p>
		</div>
	</div>
</div>